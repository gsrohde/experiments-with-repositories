name: Install pkgdown

on:
  push:

  workflow_dispatch:

jobs:
  install:
    name: Install pkgdown

    runs-on: ubuntu-latest

    steps:

    - name: 2. Set up R
      uses: r-lib/actions/setup-r@v2

    - name: Install pkgdown
      run: |
        install.packages("pkgdown")
      shell: Rscript {0}
